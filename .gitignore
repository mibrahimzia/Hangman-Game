# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
pip-wheel-metadata/

# Environment / secrets (never commit)
.env
.env.*
!.env.example
.dev.vars

# Local databases
*.db
*.db-journal
*.sqlite3
instance/

# Cloudflare Python toolchain outputs (regenerated by `pywrangler sync`)
# NOTE: only the repo-root copy is ignored. src/python_modules/ is the
# committed, self-contained runtime closure (see scripts/vendor_python_modules.py).
/python_modules/
pylock.toml
.venv-workers/
src/vendor/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Setuptools build outputs
build/
